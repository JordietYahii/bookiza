# -------------------------------------------------#
#                SUPERBOOK WRITERS                 #
# -------------------------------------------------#


# Add `~/bin` to the `$PATH`
export PATH="$HOME/bin:$PATH";

# Include modules
for file in ~/modules/.{path, init, new, insert, add, remove, length, server, check, update, help}; do
  [ -r "$file" ] && [ -f "$file" ] && source "$file";
done;
unset file;


# ---------- VERSION --------- #

version() {
  
  echo "
        Bookiza Version 0.1.0
        "
}

